<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.elderoid" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/text</item>
        <item name="colorPrimaryDark">@color/text</item>
        <item name="colorAccent">@color/text</item>
    </style>
    <style name="SwitchTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorControlActivated">@color/text</item>
        <item name="colorSwitchThumbNormal">@color/text</item>
        <item name="trackTint">@color/secondary</item>
    </style>
</resources>